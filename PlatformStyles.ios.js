import {
  StyleSheet,
} from 'react-native';

var platformStyles = StyleSheet.create({
  infoBlockContent: {
    marginTop: 16,
  },
  infoBlockOverlay: {
    backgroundColor: '#00000033',
  },
  infoBlockTemperatureText: {
    shadowColor: '#000000',
    // shadowOffset: {2, 2},

  },
  navbarPadding: {
    paddingTop: 64,
  },
})

export default platformStyles
